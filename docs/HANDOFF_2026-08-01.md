# Session handoff — 1 August 2026

This is the durable entrypoint for the next development session. It separates
verified current state from plans and hypotheses.

## Current state

The uncapped seed-reachable player crawl is complete. Bootstrap run
`8210df08-c748-4de7-9eea-ccc8740caa8a` reached truthful durable closure at
`2026-08-01 17:51:29 UTC`:

| Metric | Final value |
| --- | ---: |
| Canonical Bughouse boards | 8,195,984 |
| Public / callback-only boards | 8,195,423 / 561 |
| Participant rows | 16,391,968 |
| Discovered players | 247,274 |
| Eligible / dormant / candidate players | 1,015 / 1,153 / 245,106 |
| Completed lifetime crawls | 1,014 |
| Terminal archive-unavailable players | 1 |
| Completed durable jobs | 52,890 |
| Queued / leased / deferred / failed jobs | 0 / 0 / 0 / 0 |
| Public / callback requests | 52,175 / 2,232 |
| Database bytes | 15,145,734,144 (about 14.1 GiB) |

Migrations 0004 and 0005 were subsequently applied to the live database.
Permanent tracking starts from the 1,015-player eligible baseline; none of the
1,153 players already dormant on 1 August were retroactively enrolled. Of the
baseline cohort, 1,014 have completed lifetime crawls and one has an explicit
terminal archive-unavailable outcome, so the current-policy closure audit is
ready with zero tracked players lacking an outcome.

SQLite `PRAGMA quick_check` returned `ok`, and a full foreign-key scan returned
zero violations. The full read-only structural and data-quality audit is in
[`CRAWL_RUN_FULL_ANALYSIS.md`](CRAWL_RUN_FULL_ANALYSIS.md). Treat that report as
the final evidence record for this crawl.

Coverage means transitive closure from the approved seeds. Chess.com exposes no
global Bughouse feed, so the crawler cannot prove that a disconnected player
population does not exist.

## What the completed checkpoint implements

The crawler now includes:

- sampler version 2: one frozen deterministic public-board sample per eligible
  fully crawled player and calendar year inside the eligibility window;
- an authoritative full-archive month manifest separated from mutable monthly
  maintenance;
- explicit terminal outcomes for unavailable public months, unavailable
  archive lists, and callback probes exhausted by repeated 404s;
- a closure audit that refuses to call a drained queue complete while failed
  work or a permanently tracked player without a final outcome remains;
- an idempotent `crawl reconcile` command for historical 404s, stranded
  completion work, and legacy manifest provenance;
- a configurable five-consecutive-error circuit breaker;
- graceful `SIGTERM` handling and a systemd launcher that can preserve a fixed
  run id; and
- fixed-cutoff dormancy re-evaluation on explicit resume.

Migration `0002_partner_year_samples.sql` stores annual selections. Migration
`0003_crawl_completion_safety.sql` adds full-archive manifests and terminal
outcome fields.

Migration `0004_permanent_player_tracking.sql` adds a durable enrollment marker
and backfills it for the existing eligible cohort. Migration
`0005_correct_permanent_tracking_baseline.sql` removes the accidental initial
backfill for players already dormant when 0004 was applied. A player who
qualifies after this baseline remains in monthly game collection for life even
if they later become dormant; dormancy no longer deletes their pending work.
Monthly runs reconcile stranded lifetime work before refreshing the selected
and current months.

Legacy provenance recovery completed in run
`7acec143-9af7-4770-b209-57094a4cfdf9`: all 250 pre-migration completions fetched
their archive lists successfully, recording 13,834 authoritative manifest rows.
No missing months were found, so no game archives were re-downloaded and the
board count remained 8,195,984. The run made 250 public requests, queued no
probes, and finished with zero failed, deferred, or terminal jobs. A preceding
sandboxed attempt, run `291e2f4e-6a1e-4b89-9c2d-3defb989a152`, was interrupted
after local DNS was blocked; its retained error is environmental rather than a
Chess.com failure.

The frozen legacy indexer, read server, and frontend remain reference
implementations. They do not read `data/crawler.db` yet.

## Read-only operational checks

There is no remaining bootstrap queue to resume. Confirm current persisted
state without mutation using:

```bash
.venv/bin/bughouse-explorer crawl status
.venv/bin/bughouse-explorer crawl status --json
```

The completed run began at `2026-07-31 19:40:45 UTC`; its fixed lower
eligibility cutoff is `2025-07-31 19:40:45 UTC`. Do not resume it merely to
refresh data. Normal ongoing maintenance should use a new monthly run and its
new evaluation timestamp:

```bash
.venv/bin/bughouse-explorer crawl monthly
```

The current UTC month remains intentionally mutable. The final snapshot has
1,696 August 2026 boards through `2026-08-01 12:33:20 UTC`; later monthly
maintenance should refresh it idempotently.

## Data-quality findings to preserve

The raw corpus is healthy:

- every canonical board has exactly one white and one black participant;
- every public normalized field matches the retained Chess.com JSON;
- all UUIDs, hashes, timestamps, URLs, and JSON documents pass structural
  checks;
- all 2,232 directed partner links resolve to an existing board; and
- all 7,132,391 non-empty TCN strings are syntactically valid, representing
  325,470,732 plies. A distributed 89,101-board replay sample had zero errors.

The bounded follow-up findings are:

1. `thanithailand2024` has no stored participation and an unavailable archive,
   but is marked eligible from a game whose 2024-rated participant is actually
   `anhhungcodoc`. The qualification-owner attribution path needs correction.
2. `esquie-33` first reached 2000 ten minutes after the fixed run timestamp.
   Eligibility preserves the lower cutoff but currently lacks the matching
   upper evaluation bound.
3. The first 250 lifetime completions predate migration 0003. They have
   completed archive jobs and no unresolved months, but lack the newer
   authoritative manifest provenance.
4. Six public month endpoints are explicitly terminal unavailable. Four have
   partial coverage through opponents' archives; two belong to the unavailable
   Thailand account.
5. 1,063,593 boards have an empty source TCN. They are valid raw metadata but
   cannot contribute moves to an opening index, giving 87.02% move-data
   coverage.
6. Small upstream anomalies are faithfully retained: 53 same-account
   white/black boards, 131 ratings over 4000, 188 zero ratings, and 1,070
   one-way partner links whose targets nevertheless exist.

These findings do not require discarding or re-crawling the healthy canonical
game corpus. Downstream filters and provenance should make their treatment
explicit.

## Database and backup state

The live database is:

```text
data/crawler.db
```

The checked local recovery point is:

```text
data/crawler-final-20260801.db.zst
```

This snapshot was created with SQLite's online `.backup` command after the full
crawl, permanent-tracking correction, and legacy manifest recovery. Before
compression it matched the live database's core row counts, returned `ok` from
`PRAGMA quick_check`, and had zero foreign-key violations. The zstd stream
passed `zstd --test`; its SHA-256 is:

```text
e0fcad6e6a8b91f3cf8fb288e0abb6215b38b0adb57c9d2670f6d16245cd2d12
```

Restore it to a separate path with:

```bash
zstd -d data/crawler-final-20260801.db.zst -o /path/to/restored-crawler.db
```

The three older pre-crawl snapshots and their WAL/SHM sidecars were deleted
after this verification, reducing `data/` from 46 GiB to 17 GiB. There is still
no verified off-host backup workflow; copying this final snapshot elsewhere and
testing a restoration remains an operational priority before destructive schema
or compression work.

Never prototype compression, schema reduction, or index construction by
mutating the only live raw database. Use SQLite online backup or a checked clone
and keep the raw store recoverable.

## Stable crawler decisions

### Identity and authority

A `games` row is one Bughouse board, not the two-board match. Chess.com's board
UUID is canonical. Public monthly archives are authoritative; a callback-only
board is upgraded if it later appears publicly. Raw payloads and identifiers
remain losslessly retained.

### Eligibility

The intended threshold is inclusive at 2000 inside an exact one-calendar-year
window. Seeds are not exempt. Only public ratings and timestamped callback PGN
ratings qualify. Stored games remain when a player becomes dormant. The two
audit findings above should be fixed before treating the eligible cohort as
perfectly anchored to an evaluation instant.

### Completion and terminal outcomes

For migration-0003 completions, only months in the latest authoritative full
archive manifest govern lifetime completion. Current-month maintenance cannot
clear or block that result. An unavailable manifest month is a recorded
terminal outcome; an unavailable archive list is a different outcome and does
not claim a completed lifetime crawl.

### Annual partner probing

The version-2 choice minimizes the BLAKE2 hash of:

```text
sampler-version | normalized-username | calendar-year | board-uuid
```

The choice is frozen in `partner_year_samples`, including for a partial current
year. Probe jobs remain globally de-duplicated by board UUID. The public
opponent graph measured before conversion was connected; annual callback probes
remain a low-cost safeguard against hidden partner-only components.

### Serial HTTP and interruption

There is one synchronous request in flight across both Chess.com hosts. The
client waits at least 100 ms plus jitter between completed requests, identifies
the operator, honours `Retry-After`, and backs off on network failures, 429s,
and 5xx responses. Durable jobs and expiring leases make interruption resumable.
Five consecutive job errors stop the worker by default. `Ctrl-C` and `SIGTERM`
record graceful stopped runs.

## Recommended next work

1. Copy a checked final database snapshot off-host and verify restoration.
2. Correct qualification-owner attribution and add the upper eligibility bound,
   with regression tests and a read-only impact report before reconciling data.
3. Run the first routine permanent-tracking monthly refresh under supervision;
   it will refresh the 1,014 accessible baseline players and may discover new
   qualifiers through their new games.
4. Define derived-data policies for terminal gaps, empty TCN, rating outliers,
   self-opponent boards, callback-only boards, and one-way partner links.
5. Build the crawler-to-index adapter against a checked clone, then benchmark a
   representative derived opening index before choosing depth or hosting.
6. Evaluate lossless raw-payload compression and compact internal identifiers
   only on disposable clones, following `PLATFORM_ROADMAP.md`.
